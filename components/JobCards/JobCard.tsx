type Props = {
    jobTitle: string;
    companyName: string;
    location: string;
    moneyMin: number;
    moneyMax: number;
    time: string;
    tags: string[];
    description?: string;
};

function JobCard(props: Props) {
    return <div className=""></div>;
}

export default JobCard;
